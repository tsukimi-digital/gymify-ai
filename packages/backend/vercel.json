{
  "version": 2,
  "installCommand": "cd ../.. && npm install",
  "buildCommand": "npx prisma generate",
  "builds": [
    {
      "src": "src/vercel.ts",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "src/vercel.ts"
    }
  ]
}
